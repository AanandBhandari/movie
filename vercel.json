
{
    "builds": [
        {
        "src": "./api/server.ts",
        "use": "@vercel/node"
        }
    ],
    "routes": [
        {
        "src": "/.*",
        "destination": "/api/server.ts"
        }
    ]
}